const double SettingsIconSize = 22;
const double MinimumHeightSettingsWidget = 60;

enum WidgetDirection { ltr, rtl }
enum CheckBoxWidgetType { Box, Switch }
enum InputDialogType {
  Simple,
}

enum SelectionItem { Check, Radio }
enum SelectionDialog { SimpleCheck, SimpleRadio }
